{
  "name": "@primate/postgresql",
  "version": "0.9.0-pre",
  "description": "PostgreSQL for Primate",
  "homepage": "https://primate.run/docs/database/postgresql",
  "bugs": "https://github.com/primate-run/primate/issues",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/primate-run/primate",
    "directory": "packages/postgresql"
  },
  "files": [
    "/lib/**/*.js",
    "/lib/**/*.d.ts",
    "!/**/*.spec.*"
  ],
  "scripts": {
    "build": "npm run clean && tsgo",
    "clean": "rm -rf ./lib",
    "lint": "eslint .",
    "prepublishOnly": "npm run build",
    "test": "npm run db && npx proby",
    "db": "scripts/db.sh"
  },
  "dependencies": {
    "@primate/core": "workspace:^",
    "@rcompat/assert": "^0.14.0",
    "@rcompat/is": "^0.12.0",
    "pema": "workspace:",
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "@rcompat/type": "^0.18.0"
  },
  "peerDependencies": {
    "primate": "workspace:^"
  },
  "imports": {
    "#*": {
      "@primate/source": "./src/private/*.ts",
      "default": "./lib/private/*.js"
    }
  },
  "exports": {
    ".": {
      "@primate/source": "./src/public/index.ts",
      "browser": "./lib/public/index.client.js",
      "default": "./lib/public/index.js"
    }
  }
}
