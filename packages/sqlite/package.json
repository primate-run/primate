{
  "name": "@primate/sqlite",
  "version": "0.6.0",
  "description": "SQLite databases for Primate",
  "homepage": "https://primate.run/docs/database/sqlite",
  "bugs": "https://github.com/primate-run/primate/issues",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/primate-run/primate",
    "directory": "packages/sqlite"
  },
  "files": [
    "/lib/**/*.js",
    "/lib/**/*.d.ts",
    "!/**/*.spec.*"
  ],
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "rm -rf ./lib",
    "lint": "eslint .",
    "prepublishOnly": "npm run build",
    "test": "npm run build && npx proby",
    "test:bun": "npm run build && bun x --bun proby",
    "test:deno": "npm run build && deno run -A npm:proby"
  },
  "dependencies": {
    "@primate/core": "workspace:^",
    "@rcompat/assert": "^0.6.0",
    "@rcompat/is": "^0.4.0",
    "@rcompat/sqlite": "^0.8.0"
  },
  "devDependencies": {
    "@rcompat/type": "^0.9.0",
    "pema": "workspace:^"
  },
  "peerDependencies": {
    "primate": "workspace:^"
  },
  "imports": {
    "#*": {
      "apekit": "./src/private/*.ts",
      "default": "./lib/private/*.js"
    }
  },
  "exports": {
    ".": {
      "apekit": "./src/public/index.ts",
      "default": "./lib/public/index.js"
    }
  }
}
