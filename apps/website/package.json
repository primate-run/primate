{
  "name": "@primate/app/website",
  "type": "module",
  "scripts": {
    "build": "npx primate build",
    "serve": "npx primate serve",
    "start": "npx primate",
    "bun:build": "bunx --bun primate build",
    "bun:serve": "bunx --bun primate serve",
    "bun:start": "bunx --bun primate",
    "deps": "pnpm -r --filter={pema,@primate/core,@primate/handlebars,@primate/svelte,@primate/markdown,primate} build"
  },
  "dependencies": {
    "@plsp/svelte": "^0.1.0",
    "@primate/core": "workspace:^",
    "@primate/handlebars": "workspace:^",
    "@primate/markdown": "workspace:^",
    "@primate/svelte": "workspace:^",
    "@rcompat/fs": "^0.33.2",
    "@rcompat/http": "^0.30.0",
    "@rcompat/runtime": "^0.16.0",
    "@rcompat/string": "^0.21.0",
    "primate": "workspace:^",
    "shiki": "^4.0.2",
    "svelte": "^5.55.5"
  },
  "devDependencies": {
    "marked": "^18.0.2",
    "pema": "workspace:^"
  },
  "imports": {
    "#schemeStorage": {
      "browser": "./components/schemeStorage.client.ts",
      "default": "./components/schemeStorage.server.ts"
    }
  }
}