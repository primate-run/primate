{
  "extends": "primate/tsconfig",
  "compilerOptions": {
    "baseUrl": "${configDir}",
    "paths": {
      "@/*": [
        "*"
      ]
    }
  },
  "include": [
    "${configDir}/config",
    "${configDir}/routes",
    "${configDir}/views",
    "${configDir}/components",
    "${configDir}/stores",
    "${configDir}/test",
    "${configDir}/static"
  ],
  "exclude": [
    "node_modules"
  ]
}
